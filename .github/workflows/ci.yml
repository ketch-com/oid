name: Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
    - uses: actions/checkout@v3.0.2
    - uses: actions/cache@v3.2.1
      with:
        path: /home/runner/go
        key: v2-go-mod-${{ hashFiles('go.sum') }}
        restore-keys: v2-go-mod-
    - uses: winchci/actions-golang@v1
